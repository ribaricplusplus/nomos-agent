{
  "name": "@nomos/fake-system",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL-backed fake case system for Nomos, with a dashboard and MCP server.",
  "scripts": {
    "install:py": "uv sync",
    "dev": "uv run fake-system-web",
    "mcp": "uv run fake-system-mcp",
    "seed": "uv run fake-system-seed",
    "smoke": "uv run fake-system-smoke",
    "migrate": "uv run alembic upgrade head",
    "makemigration": "uv run alembic revision --autogenerate -m",
    "lint": "uv run ruff check ."
  }
}
