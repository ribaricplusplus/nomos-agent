# Database (SQLAlchemy URL, psycopg v3 driver)
DATABASE_URL=postgresql+psycopg://nomos:nomos@localhost:5432/fake_system

# Dashboard (FastAPI / uvicorn)
WEB_HOST=0.0.0.0
WEB_PORT=8000

# MCP server (streamable HTTP — Hermes connects here, at http://<host>:<port>/mcp)
MCP_HOST=0.0.0.0
MCP_PORT=8765
